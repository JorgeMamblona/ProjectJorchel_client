const MyTaskCard = ({
    title,
    description,
    state,
    startDate,
    endDate,
    project,
    participants
}) => {

    return (
        <h1>{title}</h1>
    )
}

export default MyTaskCard